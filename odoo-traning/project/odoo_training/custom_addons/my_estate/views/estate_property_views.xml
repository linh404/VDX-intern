<odoo>
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Estate Properties" limit="10">
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="expected_price"/>
                <field name="selling_price"
                       readonly="True"
                />
                <field name="date_availability"/>
                <field name="property_type_id"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="garden_area"/>
                <field name="total_area"/>
                <field name="best_price"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
                <header>
                    <button name="action_sold"
                            type="object"
                            string="Sold"
                            class="btn-primary"
                            invisible="state in ('sold', 'cancelled')"
                    />

                    <button name="action_open_cancel_wizard"
                            type="object"
                            string="Cancel Listing"
                            invisible="state in ('sold', 'cancelled')"/>

                    <field name="state"
                           widget="statusbar"
                           statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="property_type_id"/>
                            <field name="tag_ids"
                                   widget="many2many_tags"
                                   domain="[('id', 'in', [4,5,6])]"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"
                                   readonly="True"
                            />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="cancel_reason" readonly="1" invisible="not cancel_reason"/>
                                <field name="bedrooms"/>
                                <field name="living_area" string="Living Area (sqm)"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="salesperson_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>

                        <page string="Offers">
                            <field name="offer_ids">
                                <list editable="bottom">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="property_id"/>
                                    <field name="create_date" readonly="1"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <field name="status"/>
                                    <button name="action_accept"
                                            type="object"
                                            icon="fa-check"
                                            title="Accept Offer"/>

                                    <button name="action_refuse"
                                            type="object"
                                            icon="fa-times"
                                            title="Refuse Offer"/>
                                </list>
                            </field>
                            <button name="%(my_estate.estate_property_offer_action)d"
                                    type="action"
                                    string="Open Offers"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Properties">
                <field name="name"/>
                <field name="postcode"/>
                <field name="property_type_id"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="facades"/>
                <!--                <separator/>-->
                <field name="state"/>
                <filter
                        name="available"
                        string="Available"
                        domain="[('state', 'in', ['new', 'offer_received'])]"
                />

                <group expand="1" string="Group By">
                    <filter
                            name="postcode"
                            string="Postcode"
                            context="{'group_by': 'postcode'}"
                    />
                    <filter name="property_type"
                            string="Property Type"
                            context="{'group_by': 'property_type_id'}"/>
                </group>
                <searchpanel>
                    <field name="property_type_id" string="Property Type" icon="fa-building"/>
                    <field name="state" string="Status" icon="fa-tasks" select="multi"/>
                    <field name="tag_ids" string="Tags" icon="fa-tags" select="multi" expand="1"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!--    action window  -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">My estate property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>

    <!--    action server cancel draft property -->
    <record id="estate_property_action_cancel"
            model="ir.actions.server">
        <field name="name">Cancel Properties</field>
        <field name="model_id"
               ref="model_estate_property"/>
        <field name="binding_model_id"
               ref="model_estate_property"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">records.action_cancel_draft()</field>
    </record>

    <!--    menu    -->
    <menuitem id="estate_property_menu_action"
              name="Estate Property"
              parent="estate_menu_root"
              action="estate_property_action"
              sequence="1"/>
</odoo>
